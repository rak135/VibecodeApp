# Now

- Architecture-map and context-pack core exists.
- Relevant-file scoring hardening is implemented and tested.
- Root `AGENTS.md` exists.
- AGENTS export safety is implemented, tested, and committed.
- Generated/runtime files are ignored and are not source of truth.
- Agent-facing docs now distinguish stable root `AGENTS.md`, task-specific context packs, and ignored generated AGENTS export output.
- Protected paths policy definition is implemented with loader/schema tests.
- Context packs now render protected path edit constraints from `.vibecode/checks/protected_paths.yaml`.
- Internal git changed-file inspection utility is implemented and tested for future guard/check/run workflows.
- Internal guard rule evaluation now fails generated/runtime file changes with tests.
- Protected path guard evaluation now reports protected path scope failures, required tests, generated-artifact hard failures, and handoff/explanation requirements.
- README guard evaluation now treats root `README.md` as manual-only unless the task explicitly mentions README/docs; docs files such as `docs/QUICKSTART.md` are not blocked.
- Architecture truth guard evaluation now requires same-change handoff/history acknowledgement for `.vibecode/architecture/*.md` changes.
- Guard evaluation now warns when source and test changes are not paired, using test-map suggestions when available and allowing explicit test-only work.
- `vibecode run` orchestrates external OpenCode only when explicitly invoked, validates selected permission profiles before launch, and evaluates guard/check/handoff against the post-agent working tree.
- Default permission profiles live in committed `.vibecode/agents/{safe,fast,audit}.json`.
- Post-run guard evaluation now uses the same project-loaded protected path policy as standalone `vibecode guard`, with regression coverage for custom `.vibecode/checks/protected_paths.yaml` rules.
- AGENTS export now preserves the committed managed block content, including `PRD.json`, run metadata, and the current CLI command list.
- Root `AGENTS.md` now clarifies README is manual project documentation unless README/docs are explicitly scoped, and its generated command list follows top-level CLI help order.
- `docs/ARCHITECTURE_MAP_PRD.md` now carries a phase-boundary note at the top clarifying it describes the earlier Architecture Map Core phase; the OpenCode run adapter non-goal entry has been updated to reflect current implementation status (`vibecode run`).
- Stale-index detection now uses a disk-scan file-set fingerprint to detect added/removed tracked source files even without a new git commit; `check_index_freshness`, `cmd_run`, and `build_run_plan` all use the disk-scan fingerprint; generated/runtime paths (including `.vibecode/index/`) are excluded from the fingerprint.
- Context cards, AST parsing, and risk analysis are implemented: `schema.py` (ContextCard, RiskItem, Fact, Heuristic), `ast_parser.py` (module docstring + symbol extraction via `ast`), `risk_analyzer.py` (TODO/FIXME facts, unsafe-permission facts, high-param-count and suspicious-name heuristics), `risk_reporter.py` (writes `.vibecode/index/risk_report.json` with per-file `facts` and `heuristics` arrays). `inventory.py` now accepts `generate_cards=True/card_detail/compute_heuristics` and emits a `cards` list in `file_inventory.json`. `vibecode index` loads `.vibecode/config.yml` (`cards.detail_level`, `cards.compute_heuristics`) and writes both `file_inventory.json` (with cards) and `risk_report.json`. 50 new tests in `tests/test_vibecode_context_cards.py`; all 1310 tests pass.
- `vibecode inventory` command added: scans the repository, writes `.vibecode/index/file_inventory.json` with a `context_cards` key (one card per Python file) and `.vibecode/index/risk_report.json`. Each context card has `purpose` (module docstring), structured `symbols` list (`name`, `kind`, `line`), `content_snippet` (first 200 chars), `facts`, and `heuristics`. Heuristic items carry a `severity` field (`high_param_count` → "medium", `suspicious_name` → "low"). `risk_report.json` per-file entries have separated `facts` and `heuristics` lists, with severity included on each heuristic. `ast_parser.py` now also populates `symbol_records` (structured, used by inventory) while keeping `symbols: list[str]` for backward compat. 29 new tests in `tests/test_vibecode_inventory_cmd.py`; all 1339 tests pass.
- `vibecode serve` MCP server tools reformatted for OpenCode: `find_symbol` now returns markdown (`## Symbol: \`name\`` header, one bullet per file with kind and line number; canonical name preserved on case-insensitive match); `list_high_risk` now returns markdown (`## High-Risk Files (N)` header, one section per file with risk level, reasons, and heuristics) and broadened logic to include files with `risk_level=="high"` as well as files with `severity=="high"` heuristics. Real-project smoke: 22 high-risk files (architecture/protected docs) now surface via `list_high_risk`; `find_symbol("_git_add_commit")` reports 7 files. Config snippet command verified: `"command": "vibecode"` matches CLI entry point in `pyproject.toml`. 12 new tests added (markdown output format, high-risk-level-without-heuristics, config snippet command/args/forward-slash correctness, real-data smoke); all 1387 tests pass.
- `vibecode dashboard` TUI command implemented: `textual>=0.47` added as a dependency. `vibecode/tui_app.py` provides `VibecodeTUI` (App), `MainScreen` (DataTable of context cards: File, Purpose, Symbols), and `CardDetailScreen` (purpose, symbol table, facts, heuristics, code snippet). Footer sub-title shows total file count, card count, and high-risk item count from `risk_report.json`. Enter pushes the detail screen; Escape/Q returns. Styled via `vibecode/tui_theme.tcss` with a dark, CLI-friendly palette. `load_dashboard_data()` loads from `.vibecode/index/file_inventory.json` and `risk_report.json`. 18 new tests in `tests/test_vibecode_dashboard.py`; all 1405 tests pass.
- `load_dashboard_data()` now normalizes `symbols`, `facts`, and `heuristics` fields on each card from `None` to `[]`, preventing `TypeError` crashes in `CardDetailScreen.compose()` when a card has explicit JSON null values. 15 additional tests in `tests/test_vibecode_dashboard.py` covering null normalization, display-logic safety, and footer-counter correctness; all 1420 tests pass.
- `vibecode/data_loader.py` introduced as the single source of truth for loading `file_inventory.json` and `risk_report.json`. `ProjectData` dataclass holds raw inventory/risk dicts, normalized `cards` list, `total_files`, `high_risk_count`, and `inventory_missing`/`risk_report_missing` flags. `load_dashboard_data()` in `tui_app.py` now delegates to `load_project_data()`, removing the duplicate JSON loading logic. `VibecodeServer._load_json` now delegates JSON parsing to the shared `data_loader._load_json` helper. `cmd_serve` (MCP server) and `cmd_dashboard` (CLI) now check for missing index files and print `"Hint: … Run 'vibecode inventory' to generate them."` to stderr. Fixed pre-existing bug in `cmd_serve` where `normalise_root` was called with a `Path` object instead of a string when invoked through the CLI dispatch. 23 new integration tests in `tests/test_integration.py`; all 1432 tests pass.
- End-to-end pipeline polished for daily use with OpenCode: CLI help texts for `inventory`, `dashboard`, and `serve` now include full `description` blocks covering output files, key bindings, MCP tools, and OpenCode config snippet format. Fixed inaccurate `find_symbol` MCP tool docstring (was "JSON array", now "markdown list"). `README.md` updated with a new "Daily use with OpenCode" section covering the inventory→dashboard→serve pipeline, MCP tool reference table, and risk-report interpretation guide. `docs/QUICKSTART.md` updated: `inventory`, `dashboard`, and `serve` added to the command list; "No MCP server" removed from non-goals; Steps A/B/C added for the daily pipeline with risk-signal reference tables. `README.md` "About" section updated to list MCP server; "Built With" now lists Textual and MCP. All 1432 tests pass; ruff clean.
- Run session artifact layer implemented: `vibecode/session_log.py` provides `RunSession` dataclass with `run_dir`, all standard artifact paths (`events.jsonl`, `summary.json`, `opencode_prompt.md`, `context_pack.md`, `guard_report.json`, `guard_report.md`, `checks_report.json`, `handoff_report.json`, `handoff_report.md`, `agent_stdout.log`, `agent_stderr.log`), `ensure_dir()`, `create_event_sink()`, `snapshot_current_file()`, `snapshot_prompt()`, and `snapshot_context_pack()`. Additive — existing `.vibecode/current/` behaviour is unchanged. 36 new tests in `tests/test_vibecode_session_log.py`; all 1505 tests pass.
- P2.3 session artifact review fixes applied: `RunSession` wired into `cmd_run()` for per-run artifact paths; `_write_run_metadata()` now writes `metadata.json` under `.vibecode/runs/<session_id>/` (was flat `.json`); guard/check/handoff results, agent stdout/stderr logs, and prompt/context snapshots written to session directory alongside existing `.vibecode/current/` outputs; unused `VibecodeEvent` import removed from session-log test file; run integration tests updated to assert nested session paths; new end-to-end snapshot test added.
- `RunController` orchestration class extracted from `cmd_run` in `vibecode/run.py`. `RunController(root, task, platform, profile_name, allow_dirty, no_index, sink?, session_id?)` accepts an optional `EventSink` and orchestrates the full run lifecycle — git preflight, index check, context pack, agent invocation, post-run guard/check/handoff, summary writing — emitting structured events at each phase. New event type constants `EVENT_GIT_PREFLIGHT` and `EVENT_INDEX_CHECK` added to `vibecode/events.py`. `cmd_run` is now a thin wrapper around `RunController`. `NullEventSink` is used by default so CLI behaviour is unchanged. 21 new tests in `tests/test_vibecode_run_controller.py`; all 1527 tests pass.
- Advisory/strict guard mode for `vibecode run` implemented: `RunSummary` has a new `guard_mode: str = "advisory"` field (default). In advisory mode (default), guard errors yield `overall_status == "needs_review"` and exit code 0 — findings are fully preserved with original severity but do not block the run. In strict mode (`--guard-mode strict`), guard errors yield `"failure"` and exit code 1 (previous hard-blocking behaviour). `guard_mode` is included in `as_dict()` / `summary.json` output. `vibecode run` now accepts `--guard-mode {advisory,strict}` (default: advisory). The advisory note is printed in the run summary header when guard findings are surfaced in advisory mode. All existing guard-related tests updated; new `TestAdvisoryGuardMode` class added with 10 targeted tests. All 1549 + N tests pass (1 pre-existing failure unrelated).
- Guard report output enriched for Vibecode monitor panel: `GuardFinding` extended with `category`, `title`, `why_it_matters`, `evidence` fields. `resolved_category` derives from `rule_id` if not explicitly set (mapping: generated-runtime-files→generated-files, readme-manual-only→documentation, architecture-truth-record→architecture, source-test-change-balance→testing, protected-path-*→protected-paths). `GuardResult` gains `counts_by_severity()` and `counts_by_category()` helpers; `as_dict()` now includes `counts_by_severity` and `counts_by_category`. `write_guard_report_md()` added to `guard.py` — writes a human-readable grouped Markdown report (by severity then category) to the given path. `guard_report.md` is now written to the session run directory on every `vibecode run`. `EVENT_GUARD_FINDING = "run.guard_finding"` added to `events.py`; `RunController` emits one `GuardFindingEmitted` event per finding (with severity, rule_id, category, path, title, why_it_matters, evidence, recommended_fix, required_tests) and the `GuardCompleted` event now includes `counts_by_severity` and `counts_by_category`. 36 new tests in `tests/test_vibecode_guard_report.py`; all existing guard/run tests updated; 1599 pass (1 pre-existing failure unrelated).
- Prompt/context pack truth made first-class in observable runs: `EVENT_CONTEXT` now carries `snapshot_path` (path in run dir), `size_bytes`, `task_summary` (first 200 chars), and `sections` (list of `## ` headings from the file). `EVENT_PROMPT` now carries `snapshot_path`, `size_bytes`, `platform`, and `profile`. Snapshots are taken before events are emitted so `snapshot_path` is always present when the event fires. `_extract_context_pack_sections()` helper added to `run.py`. 20 new tests in `TestContextAndPromptSnapshotEvents` class in `tests/test_vibecode_run_controller.py`; all 1619 pass (1 pre-existing failure unrelated).
- MCP tool call instrumentation implemented: `VibecodeServer` now accepts `event_sink` and `session_id` kwargs. Each public tool method (`get_file_card`, `find_symbol`, `list_high_risk`) emits `McpToolCalled` (before), `McpToolReturned` (after, with compact summary), and `McpToolFailed` (on exception, ERROR level) events of type `run.mcp`. Result summaries are compact: `found`/`result_chars` for `get_file_card`, `match_count`/`result_chars` for `find_symbol`, `risk_count`/`result_chars` for `list_high_risk` — no full response blobs. `build_mcp_server` accepts `log_path` and `session_id` kwargs; `cmd_serve` passes `.vibecode/logs/mcp_events.jsonl` as the fallback log path and reads `VIBECODE_SESSION_ID` env var for session correlation. Private `_get_file_card`, `_find_symbol`, `_list_high_risk` hold pure logic. 22 new tests in `TestMcpToolLogging`; all 1644 pass (1 pre-existing failure unrelated).
- `vibecode monitor` operator-facing TUI command implemented: `vibecode/monitor_app.py` provides `MonitorApp` (Textual App), `TUIEventSink` (event-spine-to-TUI bridge via `call_from_thread`), and three pure formatting helpers (`route_event`, `format_agent_line`, `format_vibecode_line`). Layout: header row (repo/platform/profile), task row, split horizontal panes (agent stdout/stderr left, Vibecode events right), status bar (agent status, guard status, checks status, run artifact path), Footer with Q binding. RunController runs in a daemon thread; `TUIEventSink.emit()` routes each event to the TUI thread via `call_from_thread`. Monitor is streaming-output only (not a PTY). `vibecode/tui_theme.tcss` extended with monitor-specific CSS. `vibecode/cli.py` now registers `vibecode monitor [repo_root] [--task] [--platform] [--profile] [--allow-dirty] [--no-index] [--guard-mode]`. 45 new tests in `tests/test_vibecode_monitor.py` covering formatting helpers, TUIEventSink bridge, CLI parser registration, cmd_monitor smoke path, and dispatch; all 1694 pass (1 pre-existing failure unrelated).
- `vibecode runs` post-run inspection command implemented: `vibecode/show_run.py` provides `list_runs()`, `load_run_summary()`, `load_run_events()`, `format_run_list()`, `format_run_show()`, and `cmd_runs()`. `vibecode runs list [--repo]` lists all run session IDs most-recent-first from `.vibecode/runs/`. `vibecode runs show <session_id> [--repo] [--events]` displays task, platform/profile, start/end, exit code, guard counts-by-severity, checks status, handoff status, and existing artifact paths; `--events` replays all events from `events.jsonl` in chronological order. Missing or corrupt `events.jsonl` handled gracefully (skips bad lines, reports parse errors). Missing session ID produces a clear error with a list of available IDs. `vibecode/cli.py` registers `runs` with `list` and `show` subcommands. 40 new tests in `tests/test_vibecode_show_run.py`; 1741 pass (1 pre-existing failure unrelated).
- User-facing and agent-facing docs updated to match observable monitor reality: `README.md` gains a new "Observable run monitor" section covering `vibecode monitor` (two-pane TUI, streaming text mode, not PTY), advisory/strict guard mode, run artifact paths (`.vibecode/runs/<session_id>/`), `vibecode runs list/show`, and MCP observability limitations. `docs/QUICKSTART.md` adds `monitor` and `runs` to the command list, updates Step 8 with `--guard-mode` flag and correct artifact paths, adds Step 8b (monitor TUI), adds Step 9b (inspect previous runs), adds `runs/` to the `.vibecode/` structure reference. `AGENTS.md` and `vibecode/context/agents_export.py` now list all implemented commands (`inventory`, `dashboard`, `serve`, `monitor`, `runs list/show`). `docs/CONTROL_LAYER_FINAL_AUDIT.md` stale claims fixed: CLI row and command list updated, limitation #5 corrected, MCP/monitor limitations documented. `tests/test_vibecode_agents_export.py` conciseness limit updated from 50 to 70 lines. 1753 tests pass (1 pre-existing failure unrelated).
