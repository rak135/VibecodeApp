# Next

- Run supervised Windows dogfood using the real `vibecode` command and verify normal operator flow in an interactive terminal.
- Run a live local TUI smoke check (interactive) because this was not executed in the non-interactive P28 validation session.
- Optionally run real OpenCode smoke through the external launch path and capture any environment or provider-specific issues.
- Capture findings in handoff/history after dogfood so the next operator can see real-session results.
